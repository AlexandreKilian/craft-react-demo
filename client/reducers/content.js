export const initialState = [];


const content = (state = initialState, action) => {
    switch (action.type) {
        default: {
            return state;
        }
    }
};

export default content;
